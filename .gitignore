.DS_Store

# Raw AI shoot exports — the compressed copies the site actually serves
# live in portfolio/media/posts/. Keeping the originals out of git, since
# history is permanent and these are ~52MB of unused PNGs.
corteiz ai shoot/
arcteryx shoot ai/

# local sorting workspace (symlinks, never published)
/_sort

# local backups — not site content
/index-BEFORE-v3.html.bak
/v3.html

# raw footage staged for ingest — never published, only portfolio/media/ output is
/Iberia Eclipse

# Higgsfield working file — the keyed output in clips/fx/ is what ships
/hf_*.mp4

# the 15 clips the TV used before the 63-clip reel — kept locally as a
# fallback, but already in history under clips/visuals/, so not re-committed
/_saved/visuals-old
